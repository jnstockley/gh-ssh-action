# Contributing

Thanks for your interest in contributing!

## Development

```bash
npm install
npm test
npm run bundle
```

## Pull Requests

- Keep changes focused and small.
- Include or update tests when behavior changes.
- Run `npm test` and `npm run bundle` before opening a PR.
- Ensure `dist/` is updated and committed for action changes.
